<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.cbkj_core.mapper.LogEntityMapper">

    <insert id="insert" parameterType="com.example.cbkj_core.beans.LogEntity">
        insert into logentity (lid,url,httpMethod,ip,className,methodName,execuType,status,descr,errMsg,create_date,create_id)
        values(#{lid},#{url},#{httpMethod},#{ip},#{className},#{methodName},#{execuType},#{status},#{descr},#{errMsg},now(),#{create_id})
    </insert>

    <delete id="deleteRuleMenuByRids">


    </delete>

    <select id="getPageDatas" parameterType="com.example.cbkj_core.beans.LogEntity" resultType="Map">


    </select>

</mapper>